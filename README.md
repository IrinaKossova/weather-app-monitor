# Приложение "Погода"

Это простое приложение для отображения погоды, созданное с использованием HTML, CSS и JavaScript. Оно отображает информацию о погоде.

## Функциональность

- **Отображение погоды:** Приложение отображает данные о погоде в Москве на момент запроса.
- **Индикатор загрузки:** Визуальный индикатор отображается во время API-запросов.
- **Адаптивность для мобильных устройств:** Приложение разработано как адаптивное и приспосабливается к различным размерам экрана.

## Используемые технологии

- HTML
- CSS
- JavaScript
- Node.js (Express для сервера)

## Инструкции по установке

1. Клонируйте репозиторий:

   ```bash
   git clone <URL_репозитория>
   ```

### Локальный запуск (Без Docker)

2. Установите зависимости:

   ```bash
   npm install
   ```

3. Запустите сервер:
   ```bash
   npm start
   ```
   (Приложение будет доступно, например, на http://localhost:3000)

### Запуск с Docker Compose (Рекомендовано)

Для запуска всех сервисов (приложение, Prometheus, Grafana, Blackbox) используйте Docker Compose.

1. Убедитесь, что Docker установлен и запущен.
2. Соберите и запустите все контейнеры:
   ```bash
   docker compose up -d
   ```

## 🚀 Мониторинг и Observability (Наблюдаемость)

Проект настроен для мониторинга состояния сервиса и времени ответа API с использованием стека Prometheus и Grafana.

### Настройка Мониторинга

Все компоненты мониторинга запускаются через Docker Compose.

1.  **Prometheus:** Собирает метрики от Blackbox Exporter.
2.  **Blackbox Exporter:** Выполняет проверку доступности (HTTP-check) и замеряет время ответа API (`/health` endpoint).
3.  **Grafana:** Визуализирует собранные метрики на кастомизированном дашборде.

### Доступ к Дашборду

После запуска контейнеров дашборд доступен:

- **Grafana UI:** `http://localhost:3000` (Порт Grafana)
- **Prometheus UI:** `http://localhost:9090`

**Начальные учетные данные Grafana:**

- **Логин:** `admin`
- **Пароль:** `admin`

На дашборде "App Availability" представлены следующие метрики:

1.  **App Availability:** Доступность сервиса (0 или 1).
2.  **Time Response:** Среднее время ответа HTTP-check (в секундах).

## Лицензия

Этот проект имеет открытый исходный код.
